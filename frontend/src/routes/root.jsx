import { Layout } from "../components/Layout";
import { Header } from "../pagesComponents/index/Header";

const Root = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  );
};

export default Root;
